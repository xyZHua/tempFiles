# LinkHome Android App 测试自动化技术方案

> 版本: v1.0 | 日期: 2026-05-25

---

## 一、项目概述

### 1.1 目标

将 **74 条手工测试用例**转化为 **AI 自动化 + 纯手工**的混合测试模式，实现：

- **77% 全自动化覆盖**（57 条 A 类用例）
- **23% 手工验证**（17 条 C 类用例）
- 可复用的 Skills 工作流体系，支持后续快速迭代

### 1.2 技术选型

| 组件        | 选型                    | 原因                      |
| --------- | --------------------- | ----------------------- |
| **主框架**   | Maestro MCP           | 执行速度快、零成本、确定性高、生态成熟     |
| **辅助框架**  | Midscene              | 视觉理解能力（排序、进度条、状态图标）     |
| **AI 驱动** | Claude (opencode)     | YAML 脚本生成、执行编排、报告生成     |
| **中文输入**  | ADB Keyboard          | 绕过 MCP `inputText` 中文限制 |
| **测试设备**  | 真实 Android (49a97d48) | 生产环境验证                  |

### 1.3 架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                    test-pipeline (编排层)                    │
├─────────────────────────────────────────────────────────────┤
│  阶段0: 环境检查 → 阶段1: 用例分析 → 阶段2: 脚本生成              │
│         → 阶段3: 测试执行 → 阶段4: 报告生成                     │  
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│test-case-    │    │yaml-         │    │test-runner   │
│ analyzer     │───▶│ generator    │───▶│              │
│ (分析层)      │    │ (生成层)      │    │ (执行层)      │
└──────────────┘    └──────────────┘    └──────────────┘
        │                 │                     │
        ▼                 ▼                     ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│用例详情 MD    │    │Maestro YAML  │    │text-input    │
│分类报告 MD    │    │Midscene YAML │    │ (中文输入)     │
└──────────────┘    └──────────────┘    └──────────────┘
                              │                     │
                              ▼                     ▼
                    ┌─────────────────────────────────┐
                    │       report-generator          │
                    │         (报告层)                 │
                    └─────────────────────────────────┘
```

---

## 二、用例分类策略

### 2.1 分类标准

| 分类              | 定义                      | 数量  | 占比  |
| --------------- | ----------------------- | --- | --- |
| **A - 全AI自动化**  | 纯应用内 UI 操作 + ADB 辅助     | 57  | 77% |
| **C - 不可AI自动化** | 多设备、投屏/DLNA、网络切换、外部分享验证 | 17  | 23% |

### 2.2 C 类判定规则（不可自动化）

| 触发条件    | 原因                 | 关联用例               |
| ------- | ------------------ | ------------------ |
| 外部应用验证  | 分享到微信/QQ需打开目标应用确认  | #21, #38, #47      |
| 网络状态切换  | 断点续传需断网，影响设备连通性    | #1, #2, #66, #67   |
| 多设备协调   | Maestro 不支持多设备同时操作 | #8                 |
| 系统级服务   | Samba、VPN 需手工配置    | #9, #70            |
| 投屏/DLNA | 系统级 cast 对话框不可控    | #45, #46, #51, #52 |
| 功能入口不存在 | App 版本无"联通云盘"入口    | #12, #13, #14      |

### 2.3 A 类框架选型（Maestro vs Midscene）

**核心原则**：**"视觉判断用 Midscene，元素操作用 Maestro"**

| 优先级 | 判断条件                  | 推荐框架         | 示例       |
| --- | --------------------- | ------------ | -------- |
| P1  | 需视觉/空间/布局判断（排序、颜色、位置） | **Midscene** | #6 时光轴   |
| P2  | 动态/不可预测 UI（进度条、动画状态）  | **Midscene** | #35 上传进度 |
| P3  | 文本匹配或元素存在性检查          | **Maestro**  | 大部分用例    |
| P4  | 大量连续点击/输入/导航操作        | **Maestro**  | #54 批量上传 |

**约束**：单个 YAML 脚本只能属于一个框架，不可混用命令。

---

## 三、Skills 工作流体系

### 3.1 Skill 架构

| Skill                | 触发词      | 职责            | 输入           | 输出                    |
| -------------------- | -------- | ------------- | ------------ | --------------------- |
| `test-case-analyzer` | "分析测试用例" | 用例分类与框架选型     | Excel        | 分类报告 + 用例详情 MD        |
| `yaml-generator`     | "生成脚本"   | YAML 脚本生成     | 用例详情 MD      | Maestro/Midscene YAML |
| `test-runner`        | "执行脚本"   | 批量执行与日志记录     | YAML 目录      | test_log.md + 截图      |
| `report-generator`   | "生成报告"   | Markdown 报告生成 | test_log.md  | test_report.md        |
| `test-pipeline`      | "流水线执行"  | 全流程编排         | Excel        | 完整测试流程                |
| `text-input`         | (内部调用)   | 中文输入处理        | inputText 参数 | 输入结果验证                |

### 3.2 数据流向

```
NASAPP测试用例.xlsx
       ↓
[test-case-analyzer]
       ↓
├── test_case_analysis.md (分类报告)
└── NASAPP测试用例_YYYYMMDD_HHMMSS.md (用例详情)
       ↓
[yaml-generator]
       ↓
├── maestro_yamls/<模块>/*.yaml
└── midscene_yamls/*.yaml
       ↓
[test-runner]
       ↓
├── test_results/<批次号>/test_log.md
└── test_results/<批次号>/screenshots/
       ↓
[report-generator]
       ↓
test_results/<批次号>/test_report.md
```

### 3.3 test-pipeline 流程

```
阶段0: 环境检查
  ├─ 设备连接确认
  ├─ App 安装确认
  ├─ ADB Keyboard 检查与设置
  ├─ MCP 服务器启动
  ├─ 权限预授
  └─ 屏幕常亮设置

阶段1: 用例分析
  ├─ 读取 Excel (pandas)
  ├─ 逐条分类判定 (A/C)
  ├─ 框架选型分析
  └─ 输出分类报告 + 用例详情 MD

阶段2: 脚本生成
  ├─ 前置清理（历史残留）
  ├─ 读取用例详情 MD
  ├─ 生成 YAML（按模块目录）
  ├─ Unicode 转换（仅 Maestro）
  └─ 语法校验

阶段2.5: 数据预置（按需）
  └─ adb push 文档测试数据

阶段3: 测试执行
  ├─ 批量执行 Maestro 脚本（串行）
  ├─ 中文输入调用 text-input skill
  ├─ 失败重试 + 熔断检查
  ├─ 框架切换 → Midscene 执行
  └─ 记录执行日志

阶段4: 报告生成
  ├─ 解析 test_log.md
  ├─ 计算统计
  ├─ 趋势对比（如有历史）
  └─ 输出 test_report.md
```

---

## 四、关键技术约束与应对

### 4.1 设备兼容性约束

| 约束                          | 影响               | 应对方案                            |
| --------------------------- | ---------------- | ------------------------------- |
| **OplusHansManager 冻结驱动进程** | `launchApp` 命令失效 | 使用 `adb shell am start` 预启动 App |
| **MCP 服务器必须先启动**            | Maestro 脚本执行失败   | 阶段0 自动启动 `maestro mcp`          |

**App 启动规范**：

```powershell
# 禁止使用 Maestro launchApp
# 使用 ADB 预启动
adb -s 49a97d48 shell am start -n com.huawei.linkhome/com.huawei.netopen.homenetwork.login.activity.ApplicationEntryActivity
```

### 4.2 中文输入约束

| 问题                     | 应对方案                                |
| ---------------------- | ----------------------------------- |
| MCP `inputText` 不支持中文  | ADB Keyboard broadcast 注入           |
| `runScript` 在 MCP 下误解析 | **禁用 `runScript`**，改用 ADB broadcast |

**中文输入流程**：

```yaml
# YAML 中正常编写
- inputText: "中文文本"

# test-runner 自动调用 text-input skill
# 通过 ADB broadcast 注入：
adb shell am broadcast -a com.android.adbkeyboard.ADB_INPUT_TEXT --es msg '中文文本'

# 强制验证（UI dump 提取输入框 text 属性）
```

### 4.3 中文断言约束

Maestro `assertVisible` 中的中文需转为 Unicode 码点：

```yaml
# 原始：assertVisible: "新建相册"
# 转换后：
- assertVisible:
    text: "\u65b0\u5efa\u76f8\u518c"
```

**自动转换**：`yaml-generator` 步骤4调用 `cn2unicode.py`。

### 4.4 系统文件/照片选择器约束

| 问题                  | 应对方案             |
| ------------------- | ---------------- |
| Maestro 无法控制系统选择器内部 | 通过选择器导航到预置路径     |
| 选择器 UI 因设备而异        | 风险降级为 C 类（如执行失败） |

### 4.5 框架隔离约束

| 规则                   | 说明                                 |
| -------------------- | ---------------------------------- |
| **单个 YAML 只能属于一个框架** | 不可混用 Maestro 和 Midscene 命令         |
| Midscene 使用纯自然语言断言   | `aiAssert: "照片按时间倒序排列"`            |
| Maestro 使用精确元素匹配     | `assertVisible: { text: "\u..." }` |

---

## 五、脚本规范

### 5.1 命名与目录

```
maestro_yamls/
├── 相册管理/
│   ├── 03_new_album.yaml
│   ├── 05_new_album.yaml
│   └── ...
├── 文件管理/
│   ├── 15_long_press_file.yaml
│   └── ...
└── ...

midscene_yamls/
├── 06_timeline.yaml
├── 35_upload_progress.yaml
```

**命名规则**：`用例编号_功能简写.yaml`

### 5.2 脚本模板

```yaml
# ============================================================
# 用例编号: #3 新建相册
# 模块: 相册管理
# 测试步骤:
#   1. 点击顶部 Tab "相册"
#   2. 点击 "+" 按钮
#   ...
# 预期结果:
#   - 成功创建名为 "测试相册" 的相册
# 生成日期: YYYY-MM-DD
# ============================================================
appId: com.huawei.linkhome
---
# 家庭环境预置
- runFlow:
    when:
      notVisible:
        text: "AOM_Autotest"
    commands:
      - tapOn:
          id: com.huawei.linkhome:id/family_list_arrow
      - waitForAnimationToEnd:
          timeout: 1000
      - tapOn: "AOM_Autotest"
      - waitForAnimationToEnd:
          timeout: 3000
# 导航到目标页面
- tapOn: "iFTTR全光家庭存储"
- waitForAnimationToEnd:
    timeout: 2000
# 业务操作...
```

### 5.3 脚本自包含生命周期

```
前置检查 → 构造数据 → 执行测试 → 验证断言 → 清理数据
```

- **前置检查**：检测上次残留数据，如有则先删除
- **构造数据**：脚本内创建所需测试数据
- **清理数据**：脚本末尾必须删除自己创建的数据
- **清理失败兜底**：不影响 PASS/FAIL 判定

### 5.4 家庭环境预置

**测试家庭固定为 "AOM_Autotest"**，每条脚本开头必须执行家庭检查：

```yaml
# 首页重置：由 test-runner 通过 ADB 强制执行
# 家庭检查：脚本内通过 runFlow 条件判断
- runFlow:
    when:
      notVisible:
        text: "AOM_Autotest"
    commands:
      - tapOn:
          id: com.huawei.linkhome:id/family_list_arrow
      - tapOn: "AOM_Autotest"
```

---

## 六、执行策略

### 6.1 执行顺序

```
Maestro 脚本（串行）:
  1. 相册管理模块
  2. 共享相册模块
  3. 文件管理模块
  4. 照片管理模块
  5. 多选删除模块
  6. 回收站模块
  7. 权限/自动备份模块

Midscene 脚本（串行）:
  8. 时光轴 (#6) / 上传进度 (#35)

C 类手工：跳过执行，报告中标注
```

### 6.2 脚本前置重置

每条脚本执行前强制重置：

```powershell
# 1. 强制停止 App
adb -s 49a97d48 shell am force-stop com.huawei.linkhome

# 2. 预启动 App
adb -s 49a97d48 shell am start -n com.huawei.linkhome/...ApplicationEntryActivity

# 3. 等待加载
Start-Sleep -Seconds 3
```

### 6.3 失败重试策略

| 错误类型         | 重试次数 | 重试前处理              |
| ------------ | ---- | ------------------ |
| 元素未找到 / 断言失败 | 1 次  | 截图 → 智能首页重置 → 重新执行 |
| App 崩溃       | 1 次  | 截图 → 崩溃恢复流程 → 重新执行 |
| ADB 连接超时     | 2 次  | 确认 ADB 服务 → 重连设备   |
| 超时（>5 分钟）    | 不重试  | 记为 FAIL            |

### 6.4 失败率熔断策略

| 触发条件             | 默认阈值 | 参数控制                            |
| ---------------- | ---- | ------------------------------- |
| 连续 N 条 FAIL      | 5    | `--circuit-breaker-consecutive` |
| 当前批次失败率 > N%     | 50   | `--circuit-breaker-rate`        |
| App 崩溃恢复连续 N 次失败 | 3    | `--circuit-breaker-crash`       |

**熔断处理**：展示暂停菜单，等待用户选择（继续/重试/跳过/中止）。

### 6.5 伴侣 App 安装优化

```powershell
# 首次脚本：自动安装伴侣 App
maestro test script.yaml --device 49a97d48

# 后续脚本：跳过重装（节省 30-50 秒）
maestro test script.yaml --device 49a97d48 --no-reinstall-driver
```

---

## 七、环境检查流程

### 7.1 检查项与修复能力

| 检查项                | 获取命令                  | 修复能力      | 失败处理 |
| ------------------ | --------------------- | --------- | ---- |
| 设备连接               | `adb devices`         | 不可修复      | 终止执行 |
| App 安装             | `pm list packages`    | 不可修复      | 终止执行 |
| ADB Keyboard 安装    | `pm list packages`    | 不可修复      | 终止执行 |
| ADB Keyboard 默认输入法 | `settings get secure` | **可自动修复** | 自动设置 |
| 权限授予               | `pm grant`            | **可自动修复** | 自动授予 |
| 屏幕常亮               | `settings put system` | **可自动修复** | 自动设置 |
| MCP 服务器            | 进程检查                  | **可自动修复** | 自动启动 |

### 7.2 MCP 服务器管理

```powershell
# 检查进程
Get-Process -Name maestro -ErrorAction SilentlyContinue

# 启动 MCP（后台）
Start-Process -FilePath "maestro" -ArgumentList "mcp" -NoNewWindow

# 等待就绪（10 秒超时）
Start-Sleep -Seconds 3
```

---

## 八、测试数据策略

### 8.1 照片/视频数据源

| 场景      | 默认照片源    | 关联用例                            |
| ------- | -------- | ------------------------------- |
| 照片页签上传  | "所有照片"   | #31, #33, #34, #39, #48, #55-58 |
| 自建相册内上传 | "所有照片"   | #5, #53-62                      |
| 共享相册上传  | "手机本地照片" | #3, #4, #6                      |

**无需 adb push**：按场景区分选择源。

### 8.2 文档测试数据

| 文件类型      | 预置路径                      | 关联用例 |
| --------- | ------------------------- | ---- |
| test.docx | `/sdcard/Documents/test/` | #30  |
| test.pdf  | `/sdcard/Documents/test/` | #30  |

### 8.3 测试数据命名规范

**命名规则**：`t` + 用例编号 + 后缀字母（如 `t53a`）

| 数据类型 | 示例     |
| ---- | ------ |
| 普通相册 | `t53a` |
| 共享相册 | `t03a` |
| 文件夹  | `t19a` |

---

## 九、日志与报告格式

### 9.1 批次号规则

**格式**：`YYYY-MM-DD_NNN`（如 `2025-05-22_001`）

### 9.2 test_log.md 结构

```markdown
## 执行批次: 2025-05-22_001

### 批次元信息
| 字段 | 值 |
| 设备型号 | OPPO Find X5 Pro |
| App 版本 | 3.2.1 |
...

### 执行记录
| 开始时间 | 用例编号 | 模块 | 框架 | 结果 | 失败分类 | 截图 |

### 中文输入详情
| 开始时间 | 用例编号 | 文本 | 方式 | 验证 | 结果 |
```

### 9.3 失败分类枚举

| 分类代码                   | 说明              |
| ---------------------- | --------------- |
| `ELEMENT_NOT_FOUND`    | 选择器失效或页面未加载     |
| `TIMEOUT`              | 页面加载慢或网络延迟      |
| `ASSERTION_FAILED`     | 实际结果与预期不符       |
| `APP_CRASH`            | App 异常退出        |
| `NAVIGATION_ERROR`     | 路由错误或页面未跳转      |
| `CHINESE_INPUT_FAILED` | ADB Keyboard 问题 |
| `DEVICE_ERROR`         | 设备连接问题          |
| `SCRIPT_ERROR`         | 脚本语法错误          |
| `ENV_ERROR`            | 环境检查失败          |
| `USER_ABORT`           | 执行中被用户中止        |
| `UNKNOWN`              | 需人工分析           |

### 9.4 test_report.md 结构

```markdown
# LinkHome Android App 测试报告

## 概要
## 执行统计
## 自动化覆盖
## 按模块统计
## 失败用例详情
## 缺陷分析
## 执行趋势（如有历史）
## 手工测试结果（C 类）
```

---

## 十、目录结构

```
E:\Maestro\
├── maestro_yamls/          # Maestro YAML 脚本（按模块目录）
├── midscene_yamls/         # Midscene YAML 脚本
├── test_data/              # 测试数据源文件
├── test_results/           # 测试执行结果
│   ├── <批次号>/           # 如 2025-05-22_001
│   │   ├── test_log.md
│   │   ├── test_report.md
│   │   └── screenshots/
│   └── LATEST.txt          # 最新批次指针
├── .maestro/screenshots/   # Maestro 自动失败截图
├── .claude/skills/         # Skills 工作流定义
│   ├── test-case-analyzer/
│   ├── yaml-generator/
│   ├── test-runner/
│   ├── report-generator/
│   ├── test-pipeline/
│   ├── text-input/
│   └── shared-definitions.md
├── NASAPP测试用例.xlsx      # 测试用例源文件
├── 测试计划.md              # 完整测试计划
├── CLAUDE.md               # 项目配置文档
└── 技术方案.md             # 本文档
```

---

## 十一、风险与缓解

| 风险                  | 影响范围         | 缓解措施                  |
| ------------------- | ------------ | --------------------- |
| NAS 设备不可达           | 全部阻塞         | 执行前确认 NAS 在线、WiFi 连接  |
| ADB Keyboard 未安装    | 中文输入失败       | 阶段0 环境检查自动检测          |
| App 版本 UI 变化        | 选择器失效        | 优先 resource-id，文本匹配为辅 |
| 系统权限弹窗中断            | 用例中断         | 阶段0 权限预授              |
| OplusHansManager 问题 | launchApp 失效 | 改用 ADB am start       |
| MCP 未启动             | Maestro 执行失败 | 阶段0 自动启动 MCP          |
| Midscene API 不稳定    | 视觉验证失败       | 仅用于必要场景（#6, #35）      |

---

## 十二、执行命令速查

### 一键执行

```
触发词: "流水线执行"
```

### 分步执行

```
"分析测试用例" → test-case-analyzer
"生成脚本" → yaml-generator
"执行脚本" → test-runner
"生成报告" → report-generator
```

### 手动执行单条脚本

```powershell
# 1. 启动 MCP
Start-Process -FilePath "maestro" -ArgumentList "mcp" -NoNewWindow

# 2. 预启动 App
adb -s 49a97d48 shell am start -n com.huawei.linkhome/...ApplicationEntryActivity

# 3. 执行脚本
maestro test maestro_yamls/相册管理/03_new_album.yaml --device 49a97d48
```

---

## 十三、版本信息

| 项目                 | 版本                |
| ------------------ | ----------------- |
| 测试计划               | v1.0 (2026-04-28) |
| test-case-analyzer | v1.4.0            |
| yaml-generator     | v1.2.0            |
| test-runner        | v2.3.0            |
| report-generator   | v2.2.0            |
| test-pipeline      | v1.1.0            |
| text-input         | v1.1.0            |

---

## 十四、文档体系

| 文档                      | 职责                       |
| ----------------------- | ------------------------ |
| `测试计划.md`               | 原始手工测试用例规划与分类            |
| `CLAUDE.md`             | 项目配置、约束规则、快速参考           |
| `技术方案.md`               | 完整技术架构方案（本文档）            |
| `.claude/skills/`       | Skills 工作流详细定义           |
| `shared-definitions.md` | 跨 Skill 共享定义（模块枚举、失败分类等） |